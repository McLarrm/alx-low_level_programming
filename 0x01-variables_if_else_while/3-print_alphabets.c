#include <stdio>
/**
 * main - Printing alphabet in lower and uppercase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;
	
	if (ch = 'a'; ch <= 'z'; ch++)
	putchar(ch);
	else (ch = 'A'; ch <= 'Z'; ch++)
	putchar(ch);
	for (ch = 'a''A'; ch <= 'z''Z'; ch++)
	putchar('\n);

	return (0);
}
