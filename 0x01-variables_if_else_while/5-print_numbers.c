#include <stdio.h>

/**
 * main - Programme that prints single digit numbers
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int d;

	for (d = 0; d <= 10; d++)
	printf("%d", d);
	printf('\n');

	return (0);
}
