#include <stdio.h>

/**
 * main - Programm the prints the alphabet in lowercase 
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch = 'a';

	while (;ch <= 'z'; ch++)
	putchar(c);
	putchar('\n');

	return (0);
}
