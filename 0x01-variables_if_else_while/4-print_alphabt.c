#include <stdio.h>

/**
 * main - Programme that prints alphabets except q & e
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; c++)
	putchar(ch);

	if (ch = 'q' > 'a'; ch = 'e' > 'a')
	putchar('\n');

	return (0);
}
