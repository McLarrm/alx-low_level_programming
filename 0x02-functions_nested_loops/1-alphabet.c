#include "main.h"

/**
 * main - Function that prints the alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */
void print_alphabet(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
	{
	_putchar(c);
	}
	_putchar('\n');

	return (0);
}
